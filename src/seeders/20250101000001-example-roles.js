'use strict';

/**
 * Seeder для ролей и прав (старый формат, заменен на 20250101000007-seed-rbac.js)
 * Оставлен для обратной совместимости, но данные теперь в seed-rbac.js
 */
module.exports = {
  async up(queryInterface, Sequelize) {
    // Данные теперь в 20250101000007-seed-rbac.js
    // Этот файл оставлен пустым для обратной совместимости
  },

  async down(queryInterface, Sequelize) {
    // Данные удаляются через seed-rbac.js
  },
};

